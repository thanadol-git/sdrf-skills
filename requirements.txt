# SDRF skills — pip dependencies
# Supported Python: >=3.10, <3.13 (matches CI matrix in .github/workflows/tools-tests.yml)
# For Thermo .raw files, thermorawfileparser is not on PyPI; use conda:
#   conda install -c bioconda thermorawfileparser
#
# Install:
#   pip install -r requirements.txt

# SDRF validation and raw file analysis
sdrf-pipelines[ontology]
# techsdrf: not on PyPI or any conda channel; install from git:
#   pip install git+https://github.com/bigbio/techsdrf.git
# (fallback if that fails: git clone https://github.com/bigbio/techsdrf.git ../techsdrf && pip install ../techsdrf)

# Tools dependencies (tools/ package)
requests>=2.28
# Hardened XML parsing for scripts/europepmc_fulltext.py (XXE / entity-expansion safe)
defusedxml>=0.7

# Bundled MCP server (mcp/server.py), wired up via .mcp.json. Provides the
# PRIDE / Europe PMC / OLS tools the skills call (get_project_details,
# get_article_metadata, get_pdf_by_unpaywall, search, searchClasses, getChildren).
fastmcp>=3.4
httpx>=0.28

# Olink NPX parquet → SDRF (tools/NPXtoSDRF.py)
pyarrow>=14.0
